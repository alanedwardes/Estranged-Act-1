#!/bin/bash

# ae - this isn't working on the build rig. bypass ccache.
#exec $(dirname $0)/ccache "${DT_TOOLCHAIN_DIR}"/usr/bin/clang -Qunused-arguments "$@"
"${DT_TOOLCHAIN_DIR}"/usr/bin/clang -Qunused-arguments "$@"